{
  "database": "postgresql",
  "mode": "estimated",
  "format": "json",
  "databaseVersion": "15.19",
  "capturedAt": "2026-09-20",
  "captureCommand": "psql -h <host> -U <user> -d <test-db> -At -c \"EXPLAIN (FORMAT JSON) SELECT * FROM pd_fix_events ORDER BY total;\"",
  "sql": "SELECT * FROM pd_fix_events ORDER BY total;",
  "source": {
    "kind": "locally-generated",
    "detail": "locally generated test fixture: captured with psql against a throwaway local PostgreSQL 15.19 test database seeded by fixtures/postgres/setup.sql. Schema and rows are synthetic test data; the JSON plan is raw EXPLAIN output and was not hand-written.",
    "reference": null
  },
  "features": [
    "sort over seq scan",
    "sort incremental cost dominates the plan estimate",
    "expensive-sort rule",
    "child seq scan also triggers large-sequential-scan"
  ],
  "expect": {
    "rootNodeType": "Sort",
    "hasActualFields": false,
    "minDepth": 2,
    "findingRuleIds": [
      "expensive-sort",
      "large-sequential-scan"
    ]
  }
}
