{
  "database": "postgresql",
  "mode": "estimated",
  "format": "json",
  "databaseVersion": "15.19",
  "capturedAt": "2026-09-20",
  "captureCommand": "psql -h <host> -U <user> -d <test-db> -At -c \"EXPLAIN (FORMAT JSON) SELECT status FROM pd_fix_events GROUP BY status;\"",
  "sql": "SELECT status FROM pd_fix_events GROUP BY status;",
  "source": {
    "kind": "locally-generated",
    "detail": "locally generated test fixture: captured with psql against a throwaway local PostgreSQL 15.19 test database seeded by fixtures/postgres/setup.sql. Schema and rows are synthetic test data; the JSON plan is raw EXPLAIN output and was not hand-written.",
    "reference": null
  },
  "features": [
    "Group node",
    "parallel plan: gather merge over partial aggregate",
    "sort of 3 rows with negligible incremental cost",
    "partial seq scan also triggers large-sequential-scan"
  ],
  "expect": {
    "rootNodeType": "Group",
    "hasActualFields": false,
    "minDepth": 5,
    "findingRuleIds": [
      "large-sequential-scan"
    ],
    "hotspotNodeRefs": [
      "0.0.0.0.0",
      "0.0"
    ]
  }
}
