{
  "database": "postgresql",
  "mode": "estimated",
  "format": "json",
  "databaseVersion": "15.19",
  "capturedAt": "2026-09-20",
  "captureCommand": "psql -h <host> -U <user> -d <test-db> -At -c \"EXPLAIN (FORMAT JSON) SELECT * FROM pd_fix_events WHERE customer_id BETWEEN 100 AND 115;\"",
  "sql": "SELECT * FROM pd_fix_events WHERE customer_id BETWEEN 100 AND 115;",
  "source": {
    "kind": "locally-generated",
    "detail": "locally generated test fixture: captured with psql against a throwaway local PostgreSQL 15.19 test database seeded by fixtures/postgres/setup.sql. Schema and rows are synthetic test data; the JSON plan is raw EXPLAIN output and was not hand-written.",
    "reference": null
  },
  "features": [
    "bitmap heap scan",
    "bitmap index scan child",
    "medium selectivity index predicate",
    "no rule fires"
  ],
  "expect": {
    "rootNodeType": "Bitmap Heap Scan",
    "hasActualFields": false,
    "minDepth": 2,
    "findingRuleIds": [],
    "hotspotNodeRefs": [
      "0"
    ]
  }
}
